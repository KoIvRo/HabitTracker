<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:HabitTracker.Views"
       x:Class="HabitTracker.AppShell"
       Title="HabitTracker"
       BackgroundColor="#121212"
       FlyoutBackgroundColor="#1E1E1E"
       TitleColor="#BB86FC">

    <!-- Страницы -->
    <ShellContent Title="Главная"
                  Icon="home.png"
                  ContentTemplate="{DataTemplate views:MainPage}" />

    <ShellContent Title="Базовые привычки"
                  Icon="list.png"
                  ContentTemplate="{DataTemplate views:BaseHabitsPage}" />
</Shell>